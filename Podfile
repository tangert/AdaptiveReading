# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AdaptiveReading' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AdaptiveReading

  target 'AdaptiveReadingTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AdaptiveReadingUITests' do
    inherit! :search_paths
    # Pods for testing
  end

pod 'AttributedTextView'
pod 'SwiftCSVExport'

end
